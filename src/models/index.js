const User = require("./User");
const Card = require("./Card");


// Define relationships for User model if any
// For example, if you need to define associations (like User hasMany Orders, etc.)

// Exporting only the User model
module.exports = { User , Card};
